<UserControl
    x:Class="Clean_Reader.Controls.Components.InsideSearchBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clean_Reader.Controls.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="0,10" BorderBrush="{ThemeResource LineColor}" BorderThickness="0,0,0,1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="DisplayTextBlock" Foreground="{ThemeResource NormalTextColor}"
                     FontSize="15" FontFamily="{CustomResource Basic}" TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis"/>
        <TextBlock Grid.Row="1" Margin="0,10,0,0" FontSize="12" FontWeight="Bold" Foreground="{ThemeResource TipTextColor}"
                   HorizontalAlignment="Left" TextTrimming="CharacterEllipsis" x:Name="ChapterNameBlock"/>
    </Grid>
</UserControl>
